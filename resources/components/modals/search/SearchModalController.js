import React from 'react'

const SearchModalController = ({children}) => {
  return (
    <div className="cartmodel_controller" style={{width: '100%', height: '100%'}}>{children}</div>
  )
}

export default SearchModalController